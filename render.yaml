version: 1
services:
  - type: web
    name: venture-b-backend
    env: node
    region: oregon
    plan: starter
    buildCommand: npm install
    preDeployCommand: node runMigrations.js
    startCommand: node server.js