version: 1
services:
  - type: web
    name: venture-b-backend
    env: node
    region: oregon
    plan: starter
    buildCommand: npm install && node runMigrations.js
    startCommand: nodemon server.js
